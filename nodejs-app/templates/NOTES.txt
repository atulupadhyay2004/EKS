1. Get the application URL by running these commands:
{{- if eq .Values.service.type "ClusterIP" }}
  kubectl port-forward svc/{{ include "nodejs-app.fullname" . }} 8080:{{ .Values.service.port }}
  Then visit http://localhost:8080
{{- end }}
