apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: helm-demo-cluster
  region: ap-south-1
  version: "1.31"

managedNodeGroups:
  - name: micro-workers
    instanceType: t3.micro
    desiredCapacity: 2
    minSize: 1
    maxSize: 2
    volumeSize: 20
